# ⚡ EduManage Pro — Environment Variables
# Copy this to .env and change the values for production deployment

# Secret key for cookie signing (Change this to a random long string!)
# You can generate one using: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=edu_manage_super_secret_key_2024

# Server Settings
PORT=8000
HOST=0.0.0.0
DEBUG=True
